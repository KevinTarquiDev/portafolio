/**
 * URL de producción por defecto, usada cuando falta la variable de
 * entorno SITE_URL. Un único lugar para no repetir el literal entre
 * astro.config.mjs y el endpoint de contacto.
 */
export const DEFAULT_SITE_URL = "https://kevintarqui.vercel.app";
